# 轻断食

一个可安装到 iPhone 主屏幕的静态 PWA，用于记录轻断食时间。

## 功能

- 设置任意开始和结束时间，支持跨天禁食。
- 显示本次禁食倒计时和进度。
- 结束后记录本次禁食。
- 统计今日总禁时、近 7 天日均、每次平均和历史记录。
- 数据仅保存在当前浏览器的 localStorage。

## 发布

适合部署到 GitHub Pages，入口为仓库根目录的 `index.html`。
